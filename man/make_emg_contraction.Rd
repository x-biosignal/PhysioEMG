% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/emg-data.R
\name{make_emg_contraction}
\alias{make_emg_contraction}
\title{Create EMG PhysioExperiment with Known Contraction}
\usage{
make_emg_contraction(
  n_time = 5000,
  sr = 1000,
  contraction_start = 0.3,
  contraction_end = 0.7,
  baseline_sd = 0.01,
  contraction_sd = 0.5
)
}
\arguments{
\item{n_time}{Number of time points (default: 5000).}

\item{sr}{Sampling rate in Hz (default: 1000).}

\item{contraction_start}{Proportion of signal where contraction begins
(default: 0.3).}

\item{contraction_end}{Proportion of signal where contraction ends
(default: 0.7).}

\item{baseline_sd}{Standard deviation of baseline noise (default: 0.01).}

\item{contraction_sd}{Standard deviation of contraction activity
(default: 0.5).}
}
\value{
A \code{PhysioExperiment} object with a single \code{"raw"} assay
containing a one-channel EMG signal with a known contraction window. The
contraction region is defined by \code{contraction_start} and
\code{contraction_end} as proportions of total signal length.
}
\description{
Generates a synthetic single-channel EMG \code{PhysioExperiment} with a
clearly defined contraction period. The contraction region has substantially
higher amplitude than the baseline, making it suitable for testing onset
detection algorithms.
}
\examples{
pe <- make_emg_contraction()
pe
onset <- emgOnsetDetect(pe)
onset$onsets
}
\references{
Hodges, P.W. & Bui, B.H. (1996). "A comparison of
computer-based methods for the determination of onset of muscle contraction
using electromyography." Electroencephalography and Clinical
Neurophysiology, 101(6), 511-519. doi:10.1016/S0921-884X(96)95190-5
}
\seealso{
\code{\link[=emgOnsetDetect]{emgOnsetDetect()}} for detecting the contraction onset,
\code{\link[=emgEnvelope]{emgEnvelope()}} for extracting the amplitude envelope,
\code{\link[=make_emg]{make_emg()}} for basic multi-channel EMG,
\code{\link[=make_emg_fatigue]{make_emg_fatigue()}} for EMG with fatigue progression
}
