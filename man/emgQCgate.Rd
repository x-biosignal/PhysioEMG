% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/emg-quality.R
\name{emgQCgate}
\alias{emgQCgate}
\title{EMG Quality-Control Gate}
\usage{
emgQCgate(
  x,
  min_snr = 6,
  max_powerline = 0.1,
  max_clipping = 1,
  reject_ecg = TRUE,
  assay_name = NULL,
  ...
)
}
\arguments{
\item{x}{A PhysioExperiment object with EMG data.}

\item{min_snr}{Minimum acceptable SNR in dB (default: 6).}

\item{max_powerline}{Maximum acceptable power-line ratio (default: 0.10).}

\item{max_clipping}{Maximum acceptable clipping percentage (default: 1).}

\item{reject_ecg}{If TRUE (default), ECG-contaminated channels fail the gate.}

\item{assay_name}{Input assay name (default: first assay).}

\item{...}{Passed to \code{\link[=emgQualityCheck]{emgQualityCheck()}}.}
}
\value{
A list with \code{pass} (TRUE if every channel passes) and
\code{channels}, a data.frame with columns \code{channel}, \code{pass} and
\code{reasons} (a comma-separated string of failed criteria, or "").
}
\description{
Applies pass/fail thresholds to the \code{\link[=emgQualityCheck]{emgQualityCheck()}} metrics and reports,
per channel, whether the signal is acceptable and, if not, why.
}
\examples{
set.seed(1)
sig <- rnorm(4000, sd = 0.02); sig[1500:2500] <- rnorm(1001, sd = 0.4)
pe <- PhysioExperiment(assays = list(raw = matrix(sig, ncol = 1)),
                       samplingRate = 1000)
emgQCgate(pe)$pass
}
\references{
McManus, L., De Vito, G. & Lowery, M.M. (2020). "Analysis and
biophysics of surface EMG for physiotherapists and kinesiologists."
Frontiers in Neurology, 11, 576729. doi:10.3389/fneur.2020.576729
}
\seealso{
\code{\link[=emgQualityCheck]{emgQualityCheck()}}, \code{\link[=emgRemoveECG]{emgRemoveECG()}}
}
