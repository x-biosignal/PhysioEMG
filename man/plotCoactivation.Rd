% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/emg-plot.R
\name{plotCoactivation}
\alias{plotCoactivation}
\title{Plot Agonist-Antagonist Co-Activation}
\usage{
plotCoactivation(
  x,
  agonist,
  antagonist,
  method = c("falconer_winter", "rudolph", "frost"),
  normalize = TRUE,
  assay_name = NULL
)
}
\arguments{
\item{x}{A PhysioExperiment object of EMG envelopes.}

\item{agonist, antagonist}{Channel index or label of the two muscles.}

\item{method}{Co-contraction index for the annotation (see
\code{\link[=emgCoContraction]{emgCoContraction()}}).}

\item{normalize}{Peak-normalize each channel before plotting (default: TRUE).}

\item{assay_name}{Input assay name (default: first assay).}
}
\value{
A ggplot object.
}
\description{
Overlays the agonist and antagonist envelopes and shades their common
(co-contraction) area, annotated with the co-contraction index.
}
\examples{
t <- seq_len(1000)
a <- exp(-((t - 400) / 120)^2); b <- exp(-((t - 600) / 120)^2)
pe <- PhysioExperiment(assays = list(env = cbind(a, b)),
  colData = S4Vectors::DataFrame(label = c("TA", "GAS")), samplingRate = 1000)
plotCoactivation(pe, "TA", "GAS")
}
\references{
Falconer, K. & Winter, D.A. (1985). "Quantitative assessment of
co-contraction at the ankle joint in walking." Electromyography and Clinical
Neurophysiology, 25(2-3), 135-149.
}
\seealso{
\code{\link[=emgCoContraction]{emgCoContraction()}}
}
