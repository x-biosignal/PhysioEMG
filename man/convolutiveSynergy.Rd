% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/emg-convolutive.R
\name{convolutiveSynergy}
\alias{convolutiveSynergy}
\title{Time-varying (convolutive) muscle synergies}
\usage{
convolutiveSynergy(
  emg,
  n_synergies,
  L,
  max_iter = 300,
  tol = 1e-06,
  restarts = 3,
  seed = NULL
)
}
\arguments{
\item{emg}{A non-negative \verb{muscle x time} matrix (rows = muscles).}

\item{n_synergies}{Number of synergies.}

\item{L}{Temporal length of each synergy (samples).}

\item{max_iter}{Maximum multiplicative-update iterations (default 300).}

\item{tol}{Relative-error convergence tolerance (default 1e-6).}

\item{restarts}{Random restarts; the best is kept (default 3).}

\item{seed}{Optional integer seed.}
}
\value{
An object of class \code{"convolutive_synergy"}: a list with \code{synergies}
(a \verb{muscle x synergy x L} array), \code{activations} (\verb{synergy x time}), \code{vaf},
\code{iterations}, \code{n_synergies}, and \code{L}.
}
\description{
Extracts shift-invariant spatiotemporal muscle synergies by convolutive NMF.
Each of the \code{n_synergies} components is a \verb{muscle x L} template that the model
can place at any time with any amplitude, so a synergy captures a fixed
\emph{spatiotemporal} pattern of muscle activation (d'Avella time-varying
synergies), not just a spatial weighting.
}
\examples{
set.seed(1)
M <- 5; Tt <- 200; N <- 2; L <- 15
W0 <- array(0, c(M, N, L))
for (nn in 1:N) W0[, nn, ] <- outer(runif(M), dnorm(1:L, L / 2, 3))
H0 <- matrix(0, N, Tt); H0[1, c(30, 120)] <- 1; H0[2, c(70, 160)] <- 1
V <- matrix(0, M, Tt)
for (tau in 0:(L - 1)) V <- V +
  W0[, , tau + 1] \%*\% cbind(matrix(0, N, tau), H0[, 1:(Tt - tau), drop = FALSE])
fit <- convolutiveSynergy(V, n_synergies = 2, L = 15, seed = 1)
fit$vaf
}
\references{
d'Avella A, Saltiel P, Bizzi E (2003). Combinations of muscle synergies in the
construction of a natural motor behavior. \emph{Nat Neurosci} 6:300-308.
Smaragdis P (2004). Non-negative matrix factor deconvolution. \emph{ICA}.
}
