% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/emg-synergy-mixture.R
\name{muscleSynergyMixture}
\alias{muscleSynergyMixture}
\title{Mixture-based muscle synergy analysis}
\usage{
muscleSynergyMixture(
  list_of_data,
  n_clusters,
  n_factors,
  method = c("mfa", "mpca"),
  max_iter = 100,
  tol = 1e-04,
  n_init = 5,
  seed = NULL
)
}
\arguments{
\item{list_of_data}{A list of non-empty numeric matrices, one per subject,
each \verb{time x muscle} (rows = samples/time, columns = muscles; all subjects
share the same set of muscles/columns).}

\item{n_clusters}{Number of subgroups (mixture components).}

\item{n_factors}{Number of synergies (factors) per subgroup.}

\item{method}{\code{"mfa"} (mixture of factor analysers, per-muscle diagonal noise;
default) or \code{"mpca"} (mixture of probabilistic PCA, isotropic noise).}

\item{max_iter}{Maximum EM iterations (default 100).}

\item{tol}{Log-likelihood convergence tolerance (default 1e-4).}

\item{n_init}{Random/k-means restarts; the best log-likelihood is kept
(default 5).}

\item{seed}{Optional integer seed.}
}
\value{
An object of class \code{"synergy_mixture"}: a list with \code{cluster}
(subgroup label per subject), \code{synergies} (length-\code{n_clusters} list of
\verb{muscle x factor} loading matrices), \code{uniqueness} (per-cluster diagonal
noise), \code{mean} (per-cluster mean), \code{proportions} (mixing weights),
\code{responsibilities} (\verb{subject x cluster}), \code{loglik}, and \code{bic}.
}
\description{
Fits a mixture of factor analysers to per-subject muscle activation data to
identify subgroups of subjects with distinct muscle-synergy structure, rather
than forcing a single global synergy model on a heterogeneous population.
Each cluster's factor loading matrix is that subgroup's synergy set.
}
\examples{
set.seed(1)
M <- 6; r <- 2
LA <- matrix(abs(rnorm(M * r)), M, r)          # subgroup A synergies
LB <- matrix(abs(rnorm(M * r)), M, r)          # subgroup B synergies
gen <- function(L, n = 60) t(L \%*\% matrix(rnorm(r * n), r, n) +
                             matrix(rnorm(M * n, 0, 0.3), M, n))
data <- c(replicate(6, gen(LA), simplify = FALSE),
          replicate(6, gen(LB), simplify = FALSE))
fit <- muscleSynergyMixture(data, n_clusters = 2, n_factors = 2, seed = 1)
fit$cluster
}
\references{
Matsui Y. Mixture-Based Muscle Synergy Analysis. SSRN preprint.
\url{https://ssrn.com/abstract=6798399}

Ghahramani Z, Hinton GE (1996). The EM algorithm for mixtures of factor
analyzers. Technical Report CRG-TR-96-1, University of Toronto.

Matsui Y. synergyMixR: Mixture-Based Muscle Synergy Analysis.
\url{https://github.com/matsui-lab/synergyMixR}
}
