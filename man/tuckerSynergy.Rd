% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/emg-spacetime.R
\name{tuckerSynergy}
\alias{tuckerSynergy}
\title{Non-negative Tucker (hierarchical) muscle-synergy decomposition}
\usage{
tuckerSynergy(
  trials,
  ranks,
  max_iter = 300L,
  tol = 1e-06,
  n_restart = 3L,
  seed = NULL
)
}
\arguments{
\item{trials}{A \verb{[muscle, time, trial]} array or a list of \verb{muscle x time}
matrices.}

\item{ranks}{Length-3 factor ranks \code{c(spatial, temporal, trial)}.}

\item{max_iter, tol}{Iterations and convergence tolerance.}

\item{n_restart}{Random restarts (default 3).}

\item{seed}{Optional RNG seed.}
}
\value{
a \code{tucker_synergy}: \code{spatial} (\verb{muscle x P}), \code{temporal} (\verb{time x Q}),
\code{trial} (\verb{trial x R}), \code{core} (\verb{P x Q x R}), \code{vaf}, \code{iterations}.
}
\description{
A hierarchical tensor synergy model: factorises the \verb{muscle x time x trial}
EMG tensor into non-negative spatial, temporal and trial factor matrices plus
a CORE tensor that weights every combination of them. Unlike PARAFAC
(\code{\link[=muscleSynergyTensor]{muscleSynergyTensor()}}), the core is not diagonal, so a spatial synergy can
pair with several temporal synergies -- capturing interactions PARAFAC cannot.
}
\examples{
set.seed(1)
A <- matrix(stats::runif(6*2),6,2); B <- matrix(stats::runif(40*2),40,2); C <- matrix(stats::runif(8*2),8,2)
G <- array(stats::runif(2*2*2), c(2,2,2))
X <- reconstructTucker(G, A, B, C)
tuckerSynergy(X, ranks = c(2, 2, 2), n_restart = 1)$vaf
}
\references{
Kim YD, Choi S (2007) non-negative Tucker; Tucker LR (1966).
}
\seealso{
\code{\link[=muscleSynergyTensor]{muscleSynergyTensor()}}, \code{\link[=spaceByTimeSynergy]{spaceByTimeSynergy()}}
}
