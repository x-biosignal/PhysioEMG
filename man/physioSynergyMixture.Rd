% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/emg-synergy-mixture.R
\name{physioSynergyMixture}
\alias{physioSynergyMixture}
\title{Mixture-based muscle synergy analysis on PhysioExperiment objects}
\usage{
physioSynergyMixture(
  pe_list,
  n_clusters,
  n_factors,
  method = c("mfa", "mpca"),
  assay_name = NULL,
  ...
)
}
\arguments{
\item{pe_list}{A list with one element per subject, each a \code{PhysioExperiment}
(its assay is read) or a \verb{time x muscle} matrix.}

\item{n_clusters, n_factors}{Number of subgroups and synergies (factors).}

\item{method}{\code{"mfa"} or \code{"mpca"} (see \code{\link[=muscleSynergyMixture]{muscleSynergyMixture()}}).}

\item{assay_name}{Assay to read from each PhysioExperiment (default: its
default assay).}

\item{...}{Further arguments passed to \code{\link[=muscleSynergyMixture]{muscleSynergyMixture()}}.}
}
\value{
A \code{"synergy_mixture"} object.
}
\description{
Thin entry point that runs \code{\link[=muscleSynergyMixture]{muscleSynergyMixture()}} directly on ecosystem
data: it reads each subject's \verb{time x muscle} activation from a
PhysioExperiment (or accepts matrices) and fits the subgroup mixture.
}
\examples{
set.seed(1)
mk <- function(L) {
  pe <- PhysioCore::PhysioExperiment(
    assays = list(raw = t(L \%*\% matrix(rnorm(2 * 50), 2, 50) +
                          matrix(rnorm(6 * 50, 0, .3), 6, 50))),
    samplingRate = 100)
  pe
}
LA <- matrix(abs(rnorm(12)), 6, 2); LB <- matrix(abs(rnorm(12)), 6, 2)
pes <- c(replicate(5, mk(LA), simplify = FALSE),
         replicate(5, mk(LB), simplify = FALSE))
fit <- physioSynergyMixture(pes, n_clusters = 2, n_factors = 2, seed = 1)
fit$cluster
}
