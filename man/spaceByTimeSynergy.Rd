% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/emg-spacetime.R
\name{spaceByTimeSynergy}
\alias{spaceByTimeSynergy}
\title{Space-by-time muscle synergy decomposition (sNM3F)}
\usage{
spaceByTimeSynergy(
  trials,
  n_spatial,
  n_temporal,
  max_iter = 500L,
  tol = 1e-06,
  n_restart = 5L,
  seed = NULL
)
}
\arguments{
\item{trials}{A list of \verb{muscle x time} non-negative matrices (one per trial),
or a \verb{[muscle, time, trial]} array.}

\item{n_spatial, n_temporal}{Numbers of spatial and temporal synergies.}

\item{max_iter, tol}{Multiplicative-update iterations and convergence tolerance.}

\item{n_restart}{Random restarts (best kept; default 5).}

\item{seed}{Optional RNG seed.}
}
\value{
a \code{spacetime_synergy}: \code{spatial} (\verb{muscle x n_spatial}), \code{temporal}
(\verb{time x n_temporal}), \code{activation} (list of \verb{n_spatial x n_temporal} per
trial), \code{vaf}, \code{iterations}.
}
\description{
Decomposes a set of trials' muscle x time EMG into shared SPATIAL synergies
(which muscles group together), shared TEMPORAL synergies (when they are
active) and a per-trial activation matrix linking them (Delis et al. 2014).
}
\examples{
set.seed(1)
Ws <- matrix(stats::runif(6 * 2), 6, 2); Wt <- matrix(stats::runif(50 * 2), 50, 2)
trials <- lapply(1:10, function(n) Ws \%*\% (matrix(stats::runif(4), 2, 2)) \%*\% t(Wt))
fit <- spaceByTimeSynergy(trials, 2, 2, n_restart = 2)
fit$vaf
}
\references{
Delis I, et al. (2014) Front Comput Neurosci 8:118.
}
\seealso{
\code{\link[=muscleSynergy]{muscleSynergy()}}, \code{\link[=tuckerSynergy]{tuckerSynergy()}}
}
