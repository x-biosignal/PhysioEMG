% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/emg-seniam.R
\name{setEMGElectrode}
\alias{setEMGElectrode}
\title{Set SENIAM Electrode Metadata}
\usage{
setEMGElectrode(
  x,
  muscle,
  placement = NULL,
  ied_mm = NULL,
  reference = NULL,
  side = NULL,
  channel = NULL
)
}
\arguments{
\item{x}{A PhysioExperiment object with EMG channels.}

\item{muscle}{Muscle name(s) (character). Length 1 (applied to all targeted
channels) or one per targeted channel.}

\item{placement}{Optional electrode location text; back-filled from the
catalog when \code{NULL}.}

\item{ied_mm}{Optional inter-electrode distance in mm; back-filled from the
catalog (SENIAM default 20 mm) when \code{NULL}.}

\item{reference}{Optional reference-electrode description.}

\item{side}{Optional body side ("left"/"right"/"L"/"R"/...).}

\item{channel}{Channels to annotate: integer indices, channel labels, or
\code{NULL} (default) for all channels.}
}
\value{
The PhysioExperiment with \code{seniam_muscle},
\code{seniam_placement}, \code{seniam_orientation}, \code{seniam_ied_mm},
\code{seniam_reference} and \code{seniam_side} added to \code{colData}.
}
\description{
Attaches standardized SENIAM electrode/muscle metadata to the channels of a
PhysioExperiment, written as \code{seniam_*} columns of \code{colData}.
Muscle names are validated against \code{\link[=seniamMuscles]{seniamMuscles()}}; a recognized muscle
also back-fills its recommended electrode location, orientation and
inter-electrode distance when those are not supplied.
}
\examples{
pe <- PhysioExperiment(
  assays = list(raw = matrix(rnorm(200), ncol = 2)),
  colData = S4Vectors::DataFrame(label = c("ch1", "ch2")),
  samplingRate = 1000)
pe <- setEMGElectrode(pe, muscle = c("Biceps brachii", "Triceps brachii"),
                      side = "right")
getEMGElectrode(pe)
}
\references{
Hermens, H.J. et al. (2000). "Development of recommendations for
SEMG sensors and sensor placement procedures." Journal of Electromyography
and Kinesiology, 10(5), 361-374. doi:10.1016/S1050-6411(00)00027-4
}
\seealso{
\code{\link[=getEMGElectrode]{getEMGElectrode()}}, \code{\link[=seniamMuscles]{seniamMuscles()}}, \code{\link[=emgMFCV]{emgMFCV()}} which can use the
stored inter-electrode distance
}
