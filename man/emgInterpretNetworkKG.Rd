% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/emg-network.R
\name{emgInterpretNetworkKG}
\alias{emgInterpretNetworkKG}
\title{Interpret EMG Network with Knowledge-Graph Metadata}
\usage{
emgInterpretNetworkKG(
  network,
  node_metadata = NULL,
  kg_edges = NULL,
  threshold = NULL,
  top_n = 20L,
  window = NULL
)
}
\arguments{
\item{network}{A square matrix (channels x channels) or 3D array
(window x channels x channels).}

\item{node_metadata}{Optional data.frame containing at least \code{channel}.
Additional columns (e.g. \code{kg_node}, \code{muscle_name}, \code{muscle_group}) are
carried into the edge table.}

\item{kg_edges}{Optional data.frame of KG links. Expected columns are
\code{node_a}, \code{node_b}, and optional \code{relation}.}

\item{threshold}{Optional edge threshold. Default is 75th percentile of
upper-triangle weights.}

\item{top_n}{Maximum number of edges returned after thresholding.}

\item{window}{Optional window index when \code{network} is a 3D array.
If NULL, uses the mean across windows.}
}
\value{
A list with:
\describe{
\item{edge_table}{Ranked edge table with optional metadata/KG annotations.}
\item{threshold}{Applied threshold value.}
\item{network_matrix}{Matrix used for interpretation.}
\item{summary}{List of summary statistics.}
\item{kg_relation_summary}{Relation counts from matched KG links, or NULL.}
}
}
\description{
Adds metadata/KG context to high-weight network edges. This function does
not require a specific backend and works with user-provided tables exported
from PhysioAnnotationHub/physioKG workflows.
}
